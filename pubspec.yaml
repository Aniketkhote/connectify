name: refreshed_connect
description: "A new Flutter package project."
version: 1.0.0
homepage: null

environment:
  sdk: ">=3.2.6 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  refreshed: ^1.3.0

dev_dependencies:
  flutter_lints: ^3.0.1
  flutter_test:
    sdk: flutter

flutter: null
