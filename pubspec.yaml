name: connectify
description: "connectify is Lightweight HTTP request management package"
version: 1.0.0
repository: "https://github.com/Aniketkhote/connectify"

environment:
  sdk: ">=3.2.6 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  refreshed: ^1.4.0

dev_dependencies:
  flutter_lints: ^3.0.1
  flutter_test:
    sdk: flutter

flutter: null
